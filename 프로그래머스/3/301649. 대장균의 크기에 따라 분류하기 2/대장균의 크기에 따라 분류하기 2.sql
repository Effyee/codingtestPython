SELECT 
    ID,
    CASE
        WHEN GROUP_ID=1 THEN 'CRITICAL'
        WHEN GROUP_ID=2 THEN 'HIGH'
        WHEN GROUP_ID=3 THEN 'MEDIUM'
        ELSE 'LOW'
    END AS COLONY_NAME
FROM
    (SELECT ID,NTILE(4) OVER(ORDER BY SIZE_OF_COLONY DESC) AS GROUP_ID
FROM ECOLI_DATA)AS T
ORDER BY ID ASC;